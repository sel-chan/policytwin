# PolicyTwin Codex repair v1

Repair only the supplied fresh copy of the bundled trusted SaaS refund fixture. The canonical buggy fixture and the evaluation-only `expected-fixed` fixture are outside your workspace and must remain untouched.

This is the typed execution turn, not a planning or reporting turn. Return complete replacement content for both required workspace files: `src/refund.ts` and `tests/refund.test.mjs`. Use only the exact paths required by the supplied schema. The orchestrator writes the returned strings byte-for-byte to those two existing files and rejects any missing, empty, oversized, sensitive, or additional field.

Use the accepted policy summary, validated PolicyIR, full accepted case corpus, failing drift witnesses, approved cartography, and server-supplied bounded fixture contents. Make the smallest coherent change that fixes externally observable refund decisions. The trusted test file contains skipped server-owned D01-D03 assertions; enable those exact assertions without rewriting, weakening, relocating, or duplicating them. Do not weaken or skip tests. Do not change policy evidence, expected decisions, fixture identity, or command configuration. Do not hard-code case IDs in application logic, add a bypass, expose a secret, or alter unrelated content.

Do not run commands or use file-edit tools. Do not call shell, SDK command-execution, build, test, package-manager, network, verification, or external tools in this turn. The orchestrator alone applies the typed replacements to the fixed paths, inspects the resulting filesystem delta, maps the closed command IDs to trusted executables, and runs every accepted case through a separate server-owned evaluator. If a prior verification batch failed, use only the supplied redacted command and policy-verification evidence for the orchestrator's bounded second attempt.

Return only the strict typed edit body requested by the supplied schema. Do not output a repair summary, verification claim, shell text, Markdown fence, or extra key in this turn. A separate follow-up turn requests the strict repair report after the orchestrator observes both exact file changes.
