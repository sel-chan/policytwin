DRAFT_NOT_READY — generated from partial offline evidence; do not submit.
Implemented offline: TypeScript, Node.js, pnpm, Next.js 16, React 19, Playwright with Chrome, Node.js built-in SQLite persistence and durable RPC replay state, OpenAI Responses adapter contract, TLS 1.3 mutual-authentication transport, bounded signed external-worker supervisor/RPC contracts, a fail-only Worker RPC v2 supervisor plus strict CPU evidence v2 global-transcript/failure/signature/binding/downgrade tests, Rego source generation, OPA 1.18.2, split non-root worker/verifier/egress Dockerfiles, a shell-free canonical-local-Docker supervisor driver, fake-daemon ID/inspect/cleanup and no-restart running-instance tests, a strict fake-only three-role CPU budget ledger, cgroup-v2 observer requirements, a TLS 1.3-only no-HTTP probe, and Git.
Planned/unverified: fresh GPT-5.6 Structured Output, immutable image build and execution on a real Docker daemon, dynamic isolation, restart behavior, cgroup teardown and a real observed and signed Worker RPC v2 CPU evidence result, a real egress TLS probe, measured proxy outbound/OpenAI traffic, live Codex SDK work, dynamic SQLite container persistence, and deployment.

Worker RPC v2 transport admission is factory-identity-bound: the concrete v2 mTLS client module owns a private WeakSet, snapshots validated scalar options plus defensive copies of CA/certificate/key buffers and arrays, only its actual factory freezes and adds an admissible object, and no arbitrary registrar exists. The client rejects self-declared, v1, copied, or wrapped transports, while later caller mutation cannot redirect or corrupt the admitted connection profile; scripted response and option-mutation tests use the concrete factory over TLS 1.3 loopback. This is offline host-boundary evidence only; the supervisor remains FAIL-only and no live transport, Linux CPU proof, model call, or Codex repair is claimed.

CPU evidence v2 now has an internal synthetic-only state-machine producer. It snapshots Docker identity inputs, serializes the three-role transcript, recomputes bindings and unsigned-64 arithmetic, records overage and cleanup failures, and returns only a frozen UNSIGNED_CPU_EVIDENCE_V2_CANDIDATE wrapper with liveClaim=false and passSigningEligible=false. It rejects a port that self-declares Linux provenance and is not exported from the package root. The enclosed raw evidence remains parser-valid contract data and is not provenance or signer authorization. This is contract evidence only: no private Docker-owned system adapter, dynamically wired start barrier, finalized evidence issuer, PASS signer, real cpu.stat observation, or real containment exists.

The schema-v12 static contract retains the non-live cgroup observer requirement for an exact Docker cgroup component, cgroup-v2 filesystem, a private O_DIRECTORY/O_NOFOLLOW directory descriptor with device/inode identity, bounded descriptor-relative reads, full uint64 BigInt CPU arithmetic, descendant populated=0, explicit sample failure, initial-PID absence, original-cgroup release, and sticky normal/recovery cleanup-action failures. Offline parser and source-tamper tests cover this boundary. It has not run on Linux or Docker, still takes a post-start baseline, is not the private live adapter, and cannot authorize signing or live admission.

Schema v12 retains the internal private CPU-adapter/final-result identity boundary and adds only non-admitted implementation pieces: a host/role one-shot barrier bundled in each role image with host-owned pre-created payload/commit slots, non-replaceable paths, SHA-256 commit binding, and concurrent-release rejection; a NON_PRIVILEGED_TEST_PORT lifecycle harness with serial role-local samples and quiescent teardown accounting; and a fixed-frame C helper/client source boundary for RAW clock, pidfd, cgroup-v2 identity, freeze, kill, quiescence, and release. The barrier permission model and trusted-launcher close-before-commit order are statically tested, but actual cross-UID Docker mounts and writable-FD exclusivity remain unverified. Strict WSL compilation passed, but the available WSL cgroup root is tmpfs and runtime initialization failed closed. There is deliberately no Docker-owned system adapter, reproducible pinned helper image, finalized-evidence issuer, signer admission, PASS signing, or live gate enablement.
