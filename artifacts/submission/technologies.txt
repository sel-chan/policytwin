DRAFT_NOT_READY — generated from partial offline evidence; do not submit.
Implemented offline: TypeScript, Node.js, pnpm, Next.js 16, React 19, Playwright with Chrome, Node.js built-in SQLite persistence and durable RPC replay state, OpenAI Responses adapter contract, TLS 1.3 mutual-authentication transport, bounded signed external-worker supervisor/RPC contracts, a fail-only Worker RPC v2 supervisor plus strict candidate live-CPU proof/signature/binding/downgrade tests, Rego source generation, OPA 1.18.2, split non-root worker/verifier/egress Dockerfiles, a shell-free canonical-local-Docker supervisor driver, fake-daemon ID/inspect/cleanup and no-restart running-instance tests, a strict fake-only three-role CPU budget ledger, cgroup-v2 observer requirements, a TLS 1.3-only no-HTTP probe, and Git.
Planned/unverified: fresh GPT-5.6 Structured Output, immutable image build and execution on a real Docker daemon, dynamic isolation, restart behavior, cgroup teardown and a real signed Worker RPC v2 CPU result with global event/failure evidence, a real egress TLS probe, measured proxy outbound/OpenAI traffic, live Codex SDK work, dynamic SQLite container persistence, and deployment.

Worker RPC v2 transport admission is factory-identity-bound: the concrete v2 mTLS client module owns a private WeakSet, snapshots validated scalar options plus defensive copies of CA/certificate/key buffers and arrays, only its actual factory freezes and adds an admissible object, and no arbitrary registrar exists. The client rejects self-declared, v1, copied, or wrapped transports, while later caller mutation cannot redirect or corrupt the admitted connection profile; scripted response and option-mutation tests use the concrete factory over TLS 1.3 loopback. This is offline host-boundary evidence only; the supervisor remains FAIL-only and no live transport, Linux CPU proof, model call, or Codex repair is claimed.
