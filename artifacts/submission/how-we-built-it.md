# How we built it

DRAFT_NOT_READY — generated from partial offline evidence; do not submit.

Implemented: strict TypeScript contracts, deterministic clause segmentation and Rego generation, checksum-pinned OPA 1.18.2 compilation and evaluation, reference mutation execution, deterministic trusted-fixture overlay contracts, closed repair command IDs, SQLite-backed immutable policy/version persistence with restart recovery, versioned same-origin/CSRF-protected decision and source routes, a server-only GPT-5.6 Responses adapter contract, a real TLS 1.3 mutually authenticated external-worker transport, bounded supervisor, durable request-ID/nonce replay store, signed RPC client contract with exact tree-manifest delta validation, split non-root worker, verifier, and egress Dockerfiles and static launch contracts with explicit restart=no; a shell-free supervisor Docker driver constrained to a canonical local CLI, request/nonce-bound resource IDs, supervisor-owned inspect and cleanup checks, zero-restart enforcement, and pinned container ID/PID/start-time reobservation around worker execution; a required CPU-controller port plus a fake-only BigInt ledger that aggregates egress, worker, and verifier use and blocks receipt validation until a request- and identity-bound static proof is complete; and a contract-only Worker RPC v2 envelope with a separate signature/mTLS profile, distinct live-purpose key material, durable replay requirement, client execution binding, exact Docker or partial-attempt bindings, and required CPU evidence schema v2 with one global monotonic transcript and closed success/failure outcomes. Linux cgroup-v2 observation requirements and a TLS 1.3-only probe that sends no HTTP are also prepared. The fake proof explicitly keeps cumulative enforcement, hard-limit, overshoot-bound, and containment claims false; v2 hard-limit and overshoot claims are also false. These Docker and CPU controls are verified only by static checks, a fake daemon/controller, synthetic test-key evidence, and a loopback v2 integration that signs only typed pre-execution FAIL. The repository also includes a six-view Next.js workspace, Chrome E2E checks, blocked impact/traceability reports, SHA-256 evidence manifests, semantic claim recomputation, a closed and sensitive-content-guarded 38-file USTAR archive, a trusted Ed25519 live-attestation boundary, security/history scans, and clean-copy replay.

Not yet verified: a fresh GPT-5.6 response; immutable image builds and runtime identity on a real Docker daemon; observed worker, verifier, and egress isolation, cgroup teardown, restart behavior, and the TLS-only probe; a real controller producing freshly observed and signed Worker RPC v2 CPU evidence; real cgroup CPU sampling, polling, containment, or aggregate enforcement; proxy outbound traffic and OpenAI DNS/SNI/header behavior; live Codex SDK repair and review; production SQLite container persistence; live attestation signing; and deployment.

Worker RPC v2 transport admission is factory-identity-bound: the concrete v2 mTLS client module owns a private WeakSet, snapshots validated scalar options plus defensive copies of CA/certificate/key buffers and arrays, only its actual factory freezes and adds an admissible object, and no arbitrary registrar exists. The client rejects self-declared, v1, copied, or wrapped transports, while later caller mutation cannot redirect or corrupt the admitted connection profile; scripted response and option-mutation tests use the concrete factory over TLS 1.3 loopback. This is offline host-boundary evidence only; the supervisor remains FAIL-only and no live transport, Linux CPU proof, model call, or Codex repair is claimed.

CPU evidence v2 has an internal synthetic-only state-machine producer. It snapshots Docker identity inputs, serializes the three-role transcript, recomputes bindings and unsigned-64 arithmetic, records overage and cleanup failures, and returns only a frozen UNSIGNED_CPU_EVIDENCE_V2_CANDIDATE wrapper with liveClaim=false and passSigningEligible=false. It rejects a port that self-declares Linux provenance and is not exported from the package root. The enclosed raw evidence remains parser-valid contract data and is not provenance or signer authorization. A separate private Docker/cgroup construction now exists in source, but it has no dynamic runtime transcript or finalized evidence issuer; no PASS signer, real cpu.stat observation, or real containment is claimed.

The schema-v14 static contract retains the non-live cgroup observer requirement for an exact Docker cgroup component, cgroup-v2 filesystem, a private O_DIRECTORY/O_NOFOLLOW directory descriptor with device/inode identity, bounded descriptor-relative reads, full uint64 BigInt CPU arithmetic, descendant populated=0, explicit sample failure, initial-PID absence, original-cgroup release, and sticky normal/recovery cleanup-action failures. Offline parser and source-tamper tests cover this boundary. It has not run on Linux or Docker, still takes a post-start baseline, is distinct from the private runtime construction, and cannot authorize signing or live admission.

Schema v14 retains the private CPU-adapter/final-result identity boundary and adds a concrete non-admitted construction: a deeply frozen factory-issued supervisor lifecycle plan, owner-created and independently inspected worker-internal/outbound networks, internally derived exact role plans, one-shot barriers, a Docker-CLI-backed container owner with independent reobservation and removal/absence receipts, a full fixed-frame helper role session, a Docker/cgroup system adapter, and a dedicated private lifecycle. While a role is held, helper bind captures its baseline between two Docker observations; the cached baseline is accepted only after the second observation succeeds. The normal path then performs serial sampling, containment, quiescent final sampling, per-role Docker removal and cgroup release, helper stop, and owned-network removal. Emergency helper termination requires all Docker roles and networks absent. Private identities reject structural copies and forged role observations. Side-effect-ambiguous create failures can recover only one exact-name, exact-label owned resource through an independent cleanup signal; empty or ambiguous observations remain sticky. The barrier permission model and trusted-launcher close-before-commit order are statically tested, but actual cross-UID Docker mounts and writable-FD exclusivity remain unverified. Strict WSL compilation passed, but the available WSL cgroup root is tmpfs and runtime initialization failed closed. There is deliberately no reproducible pinned helper image, dynamic runtime transcript, finalized-evidence issuer, signer admission, PASS signing, or live gate enablement.
