# How we built it

DRAFT_NOT_READY — generated from partial offline evidence; do not submit.

Implemented: strict TypeScript contracts, deterministic clause segmentation and Rego generation, checksum-pinned OPA 1.18.2 compilation and evaluation, reference mutation execution, deterministic trusted-fixture overlay contracts, closed repair command IDs, SQLite-backed immutable policy/version persistence with restart recovery, versioned same-origin/CSRF-protected decision and source routes, a server-only GPT-5.6 Responses adapter contract, a real TLS 1.3 mutually authenticated external-worker transport, bounded supervisor, durable request-ID/nonce replay store, signed RPC client contract with exact tree-manifest delta validation, split non-root worker, verifier, and egress Dockerfiles and static launch contracts with explicit restart=no; a shell-free supervisor Docker driver constrained to a canonical local CLI, request/nonce-bound resource IDs, supervisor-owned inspect and cleanup checks, zero-restart enforcement, and pinned container ID/PID/start-time reobservation around worker execution; a required CPU-controller port plus a fake-only BigInt ledger that aggregates egress, worker, and verifier use and blocks receipt validation until a request- and identity-bound static proof is complete; and a contract-only Worker RPC v2 envelope with a separate signature/mTLS profile, distinct live-purpose key material, durable replay requirement, client execution binding, request-and-role-derived Docker binding, and strict three-role CPU proof parser/schema. Linux cgroup-v2 observation requirements and a TLS 1.3-only probe that sends no HTTP are also prepared. The fake proof explicitly keeps cumulative enforcement, hard-limit, overshoot-bound, and containment claims false; v2 hard-limit and overshoot claims are also false. These Docker and CPU controls are verified only by static checks, a fake daemon/controller, synthetic test-key proofs, and a loopback v2 integration that signs only FAIL. The repository also includes a six-view Next.js workspace, Chrome E2E checks, blocked impact/traceability reports, SHA-256 evidence manifests, semantic claim recomputation, a closed and sensitive-content-guarded 38-file USTAR archive, a trusted Ed25519 live-attestation boundary, security/history scans, and clean-copy replay.

Not yet verified: a fresh GPT-5.6 response; immutable image builds and runtime identity on a real Docker daemon; observed worker, verifier, and egress isolation, cgroup teardown, restart behavior, and the TLS-only probe; a real controller with timestamped global events and signed failure evidence producing a Worker RPC v2 result; real cgroup CPU sampling, polling, containment, or aggregate enforcement; proxy outbound traffic and OpenAI DNS/SNI/header behavior; live Codex SDK repair and review; production SQLite container persistence; live attestation signing; and deployment.
