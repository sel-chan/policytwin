# What's next

DRAFT_NOT_READY — generated from partial offline evidence; do not submit.

Complete a fresh GPT-5.6 run; provide verified immutable image digests and a canonical Docker CLI on a Linux cgroup-v2 host; implement and exercise a supervisor-owned cgroup CPU controller that produces a fresh signed Worker RPC v2 three-role result with timestamped global events and signed failure outcomes, including deliberate overage, identity drift, timeout, containment, and cleanup failures; bind that receipt into evidence v2 before changing the live gate; run the prepared web, worker/verifier, and TLS-only egress gates on a real daemon; observe proxy outbound and OpenAI upstream behavior; then execute and verify a live Codex SDK repair/review behind the authenticated supervisor transport. After that, connect live run state to the six-screen workspace, finish deployment, select a project license, record the demo, complete owner eligibility declarations, and submit. Post-challenge expansion remains outside the MVP.
